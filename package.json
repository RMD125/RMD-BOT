# Se placer au bon endroit
cd ~/RMD-BOT

# Modifier le package.json pour enlever puppeteer problématique
cat > package.json << 'EOF'
{
  "name": "rmd-bot",
  "version": "2.0.0",
  "description": "Bot WhatsApp avancé",
  "main": "main.js",
  "scripts": {
    "start": "node main.js"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0",
    "axios": "^1.6.0"
  }
}
EOF
